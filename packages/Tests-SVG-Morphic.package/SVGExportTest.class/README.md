A SVGExportTest is a collection of test methods that test the SVG export feature.

Instance Variables
	builder:		<TestSVGBuilder>

builder
	- The builder used to create the SVGs that should be exported